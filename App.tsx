import React from 'react';
import ScrollDock from './components/ScrollDock';

function App() {
    // The extension's App component now only needs to render the dock itself.
    return (
        <ScrollDock />
    );
}

export default App;
